# Dependencies / OS
node_modules/
.turbo/
dist/
apps/mobile/web-build/
apps/mobile/.expo/

# Tooling
coverage/

# Environment — never commit secrets
.env
.env.local
*.env.local
global.env.local
apps/mobile/.env
server/.env

# Wrong package managers for this repo
package-lock.json
pnpm-lock.yaml
yarn.lock

# Solana keypair files (never commit)
wallets/*.json
wallets/*.key

# Local hackathon submission draft (do not commit)
DEV3PACK_SUBMISSION.md
